// import firebase from '../Firebase';

// export async function SummaryMenuCollection(params) {
//   const summaryList = [];

//   const snapshot = await firebase.firestore().collection('summaryMenu').get();

//   snapshot.forEach((doc) => {
//     summaryList.push(doc.data());
//   });

//   params(summaryList);
// }
